<?xml version="1.0" encoding="utf-8"?>
<Window
	x:Class="UiaSpy.MainWindow"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:local="using:UiaSpy"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:models="using:UiaSpy.Models"
	mc:Ignorable="d"
	Title="UiaSpy">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/> <!--Contains the TreeView -->
		</Grid.RowDefinitions>
		<MenuBar Grid.Row="0">
			<MenuBarItem Title="File">
				<MenuFlyoutItem Text="Exit"/>
			</MenuBarItem>
			<MenuBarItem Title="Options">
				<MenuFlyoutItem Text="Preferences"/>
			</MenuBarItem>
			<MenuBarItem Title="Help">
				<MenuFlyoutItem Text="About"/>
			</MenuBarItem>
		</MenuBar>
		<Grid Grid.Row="1">
			<Grid>
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="*"/>
					<ColumnDefinition Width="Auto"/>
					<ColumnDefinition Width="Auto"/>
				</Grid.ColumnDefinitions>
				<TextBox Grid.Column="0" x:Name="pathToExe" Text="{x:Bind Path=ExePath.Value, Mode=TwoWay}" PlaceholderText="Path to executable" AcceptsReturn="false"/>
				<Button Grid.Column="1" x:Name="browseExePath" Click="browseButton_Click" Content="Browse..."/>
				<Button Grid.Column="2" x:Name="launchExe" Click="launchExeButton_Click" Content="Run and attach"/>
			</Grid>
		</Grid>
		<Grid Grid.Row="2">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="400"/> <!-- TreeView -->
				<ColumnDefinition Width="Auto"/> <!-- Element details -->
			</Grid.ColumnDefinitions>
			<Border Grid.Column="0" BorderBrush="Black" BorderThickness="2" CornerRadius="5" Padding="5">
				<TreeView x:Name="UiElement_UiaTreeTreeView" ItemsSource="{x:Bind Path=UiaTreeEntries, Mode=OneWay}" SelectionChanged="potato">
					<TreeView.ItemTemplate>
						<DataTemplate x:DataType="models:UiaTreeEntry">
							<TreeViewItem Content="{x:Bind Name, Mode=OneWay}" ItemsSource="{x:Bind Children, Mode=OneWay}"/>
						</DataTemplate>
					</TreeView.ItemTemplate>
				</TreeView>
			</Border>
			<StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
				<Button x:Name="myButton" Click="myButton_Click">Click Me</Button>
			</StackPanel>
		</Grid>
	</Grid>
</Window>
